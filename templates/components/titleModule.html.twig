<div class="d-flex justify-content-between align-items-center mb-4">
	<h1 class="h3">{{module.title ?? 'Module title'}}</h1>
	{% if module.action is defined %}
		<a class="btn btn-success" id="{{module.id}}">{{module.action}}</a>
	{% endif %}
</div>